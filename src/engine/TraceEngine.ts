import{
    ExtensionContext,
    Disposable,
    commands,
    TextEditor,
    TextEditorEdit,
} from 'vscode';

export class TraceEngine{
    constructor(
        private readonly context: ExtensionContext
    ){
        
    };

    

}